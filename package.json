{
  "name": "@heathprovost/yocal",
  "version": "1.0.0",
  "description": "",
  "main": "_site/index.html",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "eleventy --serve --quiet --port=5000 --incremental",
    "build": "eleventy --quiet",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
